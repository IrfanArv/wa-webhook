let sampleMessage = require('./sample')
let startBot = require('./started')
let endChat = require('./endChat')
let WebHook = require('./hook')

module.exports = {
    sampleMessage,
    startBot,
    endChat,
    WebHook,
}
